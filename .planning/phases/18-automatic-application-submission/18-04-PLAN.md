---
phase: 18-automatic-application-submission
plan: 04
type: tdd
wave: 2
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - agents/lever_portal.py
  - agents/spec/lever-portal.md
  - tests/test_lever_portal.py
autonomous: true
requirements:
  - LEVER-01
must_haves:
  truths:
    - "lever_portal.run() POSTs multipart/form-data to api.lever.co/v0/postings/{company}/{posting_id}/apply"
    - "lever_portal.run() on HTTP 429 reads the Retry-After header (default 60s) and retries exactly once"
    - "lever_portal.run() on HTTP 400 raises AgentError immediately without retry (per CONTEXT decision)"
    - "lever_portal.run() captures applicationId from response JSON and writes it to application_submissions"
    - "lever_portal.run() sets jobs.status to submitting before POST and submitted or submission_failed after"
    - "lever_portal.run() deletes temp PDF in all exit paths"
    - "lever_portal.run() writes pipeline_events for every outcome"
  artifacts:
    - path: "agents/lever_portal.py"
      provides: "Lever Postings API portal agent"
      exports: ["run", "AgentError"]
    - path: "agents/spec/lever-portal.md"
      provides: "Canonical implementation contract for Lever portal"
    - path: "tests/test_lever_portal.py"
      provides: "TDD tests for lever_portal.run()"
  key_links:
    - from: "agents/lever_portal.py"
      to: "utils/resume_render.py"
      via: "calls render_resume_to_pdf before multipart POST"
      pattern: "render_resume_to_pdf"
    - from: "agents/lever_portal.py"
      to: "database/010_application_submissions.sql"
      via: "INSERT INTO application_submissions with ats_type='lever', applicationId from response"
      pattern: "application_submissions"
---

<objective>
Implement the Lever portal agent with full TDD coverage. Lever differs from Greenhouse in: no pre-flight GET for questions (form fields are fixed), 429 rate-limiting with Retry-After header, and a different response schema (applicationId, not id).

Purpose: Wave 2 — depends on the DB schema (18-01) and resume render/answer utilities (18-02). Runs in parallel with 18-03 (Greenhouse) and 18-05 (Ashby) since no shared files are modified.

Output:
- agents/lever_portal.py — ephemeral agent
- agents/spec/lever-portal.md — implementation contract
- tests/test_lever_portal.py — TDD tests
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-automatic-application-submission/18-CONTEXT.md
@agents/confirmation.py
@agents/greenhouse_portal.py
@utils/resume_render.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for lever_portal.run()</name>
  <files>tests/test_lever_portal.py</files>
  <read_first>
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (Lever API details: POST url, 429 retry, Retry-After header, applicationId capture)
    - agents/confirmation.py (agent signature and AgentError pattern)
    - tests/test_greenhouse_portal.py (mock pattern to follow for consistency)
  </read_first>
  <behavior>
    - test_lever_run_posts_to_correct_url: mock httpx.post returns {"applicationId": "lev-abc"}; assert called with "https://api.lever.co/v0/postings/myco/post-123/apply"
    - test_lever_run_success_returns_submitted_outcome: mock POST returns {"applicationId": "lev-abc"}; assert return dict has outcome="submitted", ats_application_id="lev-abc"
    - test_lever_run_retries_on_429_with_retry_after_header: first call returns status 429 with Retry-After="2" header; second call returns {"applicationId": "lev-xyz"}; assert httpx.post called twice; assert time.sleep called with 2.0
    - test_lever_run_retries_on_429_with_default_60s_when_no_retry_after_header: first call returns 429 with no Retry-After; second call succeeds; assert time.sleep called with 60
    - test_lever_run_does_not_retry_on_400: httpx.post returns status 400; assert httpx.post called exactly once; assert status set to submission_failed
    - test_lever_run_sets_status_submitting_before_post: assert DB update to submitting happens before httpx.post call
    - test_lever_run_sets_status_submitted_on_success: assert DB update to submitted after successful POST
    - test_lever_run_sets_status_submission_failed_on_http_error: httpx.post raises httpx.HTTPStatusError with status 500 after retry; assert status = submission_failed
    - test_lever_run_deletes_temp_pdf_on_success: assert temp PDF file deleted after successful upload
    - test_lever_run_deletes_temp_pdf_on_failure: httpx.post raises; assert temp PDF still deleted
    - test_lever_run_writes_application_submissions_row: mock DB; assert INSERT INTO application_submissions with ats_type='lever'
  </behavior>
  <action>
    Create tests/test_lever_portal.py. Import run and AgentError from agents.lever_portal — ImportError confirms RED. Write all 11 behavior tests. Mock httpx.post, utils.resume_render.render_resume_to_pdf, time.sleep, and psycopg2.connect at outer boundary. Commit: "test(18-04): add failing tests for lever_portal.run()"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/test_lever_portal.py --tb=short 2>&1 | grep -E "ERROR|FAILED|ImportError" | head -5</automated>
  </verify>
  <done>pytest reports ImportError for agents.lever_portal — tests are RED.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement agents/lever_portal.py and spec</name>
  <files>agents/lever_portal.py, agents/spec/lever-portal.md</files>
  <read_first>
    - tests/test_lever_portal.py (the failing tests — implement exactly what they test)
    - agents/greenhouse_portal.py (agent structure and helper functions to mirror)
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (Lever API, D-01, D-09 through D-11, 429 retry behavior)
  </read_first>
  <action>
    Create agents/spec/lever-portal.md with sections: Purpose, Trigger, Pre-conditions, API Details, Steps, DB Writes, Status Transitions, Error Handling, 429 Retry Policy, Model.

    Create agents/lever_portal.py:

    Module-level constants:
    - LEVER_APPLY_URL = "https://api.lever.co/v0/postings/{company}/{posting_id}/apply"
    - MODEL = "google/gemini-3.1-flash-lite"
    - DEFAULT_RETRY_AFTER = 60  (seconds, when Retry-After header absent on 429)

    Helpers _get_conn(), _notify(), AgentError mirror confirmation.py pattern.

    Function run(job_id: str, model: str = MODEL, event_callback: EventCallback = None) -> dict:
      Step 1: DB reads — jobs: ats_board_token, ats_posting_id, jd_text, active_resume_id. resume_versions: latex_source. user_profile id=1: full_name, email, phone, location, linkedin_url, github_url, portfolio_url, salary_expectation.

      Step 2: Validate and set jobs.status = 'submitting' (approved→submitting transition).

      Step 3: render_resume_to_pdf(yaml_content=latex_source) → pdf_bytes. EnvironmentError → status='submission_failed', AgentError. Temp PDF path stored for cleanup in finally block.

      Step 4: Build multipart payload. Fixed Lever fields: name (full_name), email, phone, location, urls[LinkedIn], urls[GitHub], urls[Portfolio], comments="", resume (pdf_bytes as file tuple). Lever uses name/email not first/last split.

      Step 5: POST to LEVER_APPLY_URL.format(company=ats_board_token, posting_id=ats_posting_id) with files= multipart. Handle:
        - 429: read Retry-After header; default to DEFAULT_RETRY_AFTER; time.sleep(retry_after); retry once.
        - 400: do NOT retry; raise immediately with status='submission_failed'.
        - 5xx (not 429): single retry with 5s sleep.
        - Other HTTP errors: raise AgentError.

      Step 6: Parse response.json(). Extract applicationId = response_json.get("applicationId"). Write application_submissions(status='submitted', ats_type='lever', ats_application_id=applicationId). Set jobs.status='submitted'. Write pipeline_events. Return {"outcome": "submitted", "job_id": job_id, "ats_type": "lever", "ats_application_id": applicationId}.

      Finally block: delete temp PDF.

    Run all 11 lever tests. Commit: "feat(18-04): implement lever_portal agent"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/test_lever_portal.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 11 lever_portal tests pass. agents/lever_portal.py and agents/spec/lever-portal.md exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lever API 429 response → time.sleep | Retry-After header value from untrusted API controls sleep duration |
| user_profile PII → multipart POST | Email, phone, location leave the system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Denial of Service | Retry-After header from Lever | mitigate | Cap Retry-After at 120s maximum; default to 60s if absent; only one retry |
| T-18-04-02 | Information Disclosure | PII in Lever POST body | accept | Lever is the intended recipient; no local logging of POST body |
| T-18-04-03 | Information Disclosure | error_detail in DB | mitigate | truncate Lever error response to first 500 chars |
| T-18-04-SC | Tampering | No new package installs | accept | httpx and time are already available |
</threat_model>

<verification>
1. uv run pytest tests/test_lever_portal.py -v — all 11 tests pass
2. python -c "from agents.lever_portal import run, AgentError; print('OK')"
3. grep "api.lever.co" agents/lever_portal.py returns the URL constant
4. grep "Retry-After" agents/lever_portal.py shows header read logic
</verification>

<success_criteria>
- 429 with Retry-After header → sleep(header value) then retry
- 429 without Retry-After header → sleep(60) then retry
- 400 → immediate failure, no retry
- Temp PDF deleted in all exit paths
- application_submissions row written for every outcome
- 11 passing pytest tests
</success_criteria>

<output>
Create .planning/phases/18-automatic-application-submission/18-04-SUMMARY.md when done
</output>
