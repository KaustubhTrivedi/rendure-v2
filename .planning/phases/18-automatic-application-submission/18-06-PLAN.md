---
phase: 18-automatic-application-submission
plan: 06
type: tdd
wave: 3
depends_on:
  - "18-03"
  - "18-04"
  - "18-05"
files_modified:
  - agents/portal_router.py
  - agents/spec/portal-router.md
  - agents/orchestrator.py
  - run_agents.py
  - agents/__init__.py
  - tests/test_portal_router.py
autonomous: true
requirements:
  - PORTAL-ROUTER-01
  - ORCHESTRATOR-WIRING-01
must_haves:
  truths:
    - "portal_router.run() dispatches to greenhouse_portal.run() when jobs.ats_type='greenhouse'"
    - "portal_router.run() dispatches to lever_portal.run() when jobs.ats_type='lever'"
    - "portal_router.run() dispatches to ashby_portal.run() when jobs.ats_type='ashby'"
    - "portal_router.run() for unsupported ats_type sets jobs.status='submission_failed' and writes pipeline_events with human-readable reason (per D-08)"
    - "portal_router.run() first calls detect_ats(job_url) to populate jobs.ats_type/ats_board_token/ats_posting_id before dispatching"
    - "run_agents.py accepts --auto-apply flag; when absent, orchestrator never calls portal_router.run()"
    - "orchestrator.run() calls portal_router.run() after confirmation agent succeeds when auto_apply=True"
    - "agents/__init__.py exports run_portal_router"
  artifacts:
    - path: "agents/portal_router.py"
      provides: "ATS dispatch router, detect_ats call, unsupported ATS handling"
      exports: ["run", "AgentError"]
    - path: "agents/spec/portal-router.md"
      provides: "Canonical implementation contract for portal router"
    - path: "agents/orchestrator.py"
      provides: "Modified to accept auto_apply param, call portal_router after confirmation"
    - path: "run_agents.py"
      provides: "Modified to add --auto-apply argparse flag"
    - path: "agents/__init__.py"
      provides: "Updated exports including run_portal_router"
    - path: "tests/test_portal_router.py"
      provides: "TDD tests for portal_router.run() dispatch logic"
  key_links:
    - from: "agents/portal_router.py"
      to: "utils/ats_detect.py"
      via: "calls detect_ats(job_url) and writes ats_type, ats_board_token, ats_posting_id to jobs row before dispatch"
      pattern: "detect_ats"
    - from: "agents/orchestrator.py"
      to: "agents/portal_router.py"
      via: "imports portal_router.run and calls _spawn_with_fallback after confirmation when auto_apply=True"
      pattern: "portal_router"
    - from: "run_agents.py"
      to: "agents/orchestrator.py"
      via: "passes auto_apply=args.auto_apply to orchestrator.run()"
      pattern: "auto_apply"
---

<objective>
Wire the portal router into the orchestrator and add the --auto-apply CLI flag. This is the final plan: it integrates all portal agents behind a single dispatch point and gates the whole flow behind an explicit opt-in flag.

Purpose: Portal agents exist but are unreachable until the router and orchestrator wiring are in place. This plan makes the end-to-end path functional.

Output:
- agents/portal_router.py — detects ATS, dispatches to correct portal agent, handles unsupported ATS
- agents/spec/portal-router.md — implementation contract
- agents/orchestrator.py (modified) — auto_apply param, portal_router call after confirmation
- run_agents.py (modified) — --auto-apply flag
- agents/__init__.py (modified) — export run_portal_router
- tests/test_portal_router.py — TDD tests
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-automatic-application-submission/18-CONTEXT.md
@agents/orchestrator.py
@agents/confirmation.py
@agents/__init__.py
@run_agents.py
@utils/ats_detect.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for portal_router.run() dispatch</name>
  <files>tests/test_portal_router.py</files>
  <read_first>
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (D-07 auto-apply opt-in, D-08 unsupported ATS handling, portal router dispatch rules)
    - agents/confirmation.py (run signature pattern)
    - utils/ats_detect.py (ATSInfo dataclass — returned by detect_ats)
    - tests/test_greenhouse_portal.py (mock pattern)
  </read_first>
  <behavior>
    - test_portal_router_dispatches_to_greenhouse_when_ats_type_is_greenhouse: mock detect_ats returns ATSInfo(ats_type="greenhouse", ...); mock DB job_url; mock greenhouse_portal.run returns {"outcome": "submitted"}; assert greenhouse_portal.run called once
    - test_portal_router_dispatches_to_lever_when_ats_type_is_lever: same pattern, lever_portal.run called
    - test_portal_router_dispatches_to_ashby_when_ats_type_is_ashby: same pattern, ashby_portal.run called
    - test_portal_router_sets_submission_failed_for_unknown_ats_type: detect_ats returns ATSInfo(ats_type="unknown"); assert jobs.status updated to submission_failed; assert pipeline_events INSERT with human-readable detail containing "ATS not supported"; assert portal agents NOT called; assert AgentError raised
    - test_portal_router_writes_ats_fields_to_jobs_before_dispatch: detect_ats returns ATSInfo(ats_type="greenhouse", board_token="acme", posting_id="123"); assert UPDATE jobs SET ats_type='greenhouse', ats_board_token='acme', ats_posting_id='123' executed before portal run
    - test_portal_router_reads_job_url_from_db: mock DB cursor returning job_url; assert detect_ats called with that url
    - test_portal_router_propagates_portal_agent_result: greenhouse_portal.run returns {"outcome": "submitted", "ats_application_id": "gh-99"}; assert portal_router.run returns same dict
    - test_portal_router_sets_submission_failed_when_portal_agent_raises_agent_error: greenhouse_portal.run raises AgentError("some error"); assert portal_router does not swallow the exception; status = submission_failed before re-raise
  </behavior>
  <action>
    Create tests/test_portal_router.py. Import run and AgentError from agents.portal_router — ImportError confirms RED. Write all 8 behavior tests. Mock:
    - utils.ats_detect.detect_ats (return ATSInfo instances)
    - agents.greenhouse_portal.run, agents.lever_portal.run, agents.ashby_portal.run (portal dispatch targets)
    - psycopg2.connect (DB boundary)
    Commit: "test(18-06): add failing tests for portal_router.run()"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/test_portal_router.py --tb=short 2>&1 | grep -E "ERROR|FAILED|ImportError" | head -5</automated>
  </verify>
  <done>pytest reports ImportError for agents.portal_router — tests are RED.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement portal_router, spec, orchestrator wiring, CLI flag</name>
  <files>agents/portal_router.py, agents/spec/portal-router.md, agents/orchestrator.py, run_agents.py, agents/__init__.py</files>
  <read_first>
    - tests/test_portal_router.py (the failing tests — implement exactly what they test)
    - agents/orchestrator.py (full file — run() signature, _spawn_with_fallback call, step 8 Confirmation section to extend)
    - run_agents.py (full file — argparse setup to extend with --auto-apply)
    - agents/__init__.py (current exports to extend)
    - agents/confirmation.py (agent pattern to mirror for portal_router)
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (D-07 opt-in, D-08 unsupported ATS, detect_ats usage)
    - utils/ats_detect.py (ATSInfo.ats_type values: "greenhouse", "lever", "ashby", "unknown")
  </read_first>
  <action>
    Create agents/spec/portal-router.md with sections: Purpose, Trigger, Steps, Dispatch Table, Unsupported ATS Handling, DB Writes, Model.

    Create agents/portal_router.py:
    - Helpers _get_conn(), _notify(), AgentError (same pattern as all other agents).
    - DISPATCH_TABLE = {"greenhouse": greenhouse_portal_run, "lever": lever_portal_run, "ashby": ashby_portal_run} — import each portal's run function as an alias at module top.
    - MODEL = "google/gemini-3.1-flash-lite"
    - Function run(job_id: str, model: str = MODEL, event_callback: EventCallback = None) -> dict:
        Step 1: Read job_url from jobs WHERE job_id.
        Step 2: ats_info = detect_ats(job_url).
        Step 3: UPDATE jobs SET ats_type=%s, ats_board_token=%s, ats_posting_id=%s WHERE job_id=%s with ats_info values (parameterized).
        Step 4: If ats_info.ats_type == "unknown": UPDATE jobs SET status='submission_failed'; INSERT pipeline_events with detail="ATS not supported — URL did not match Greenhouse, Lever, or Ashby patterns. URL: {job_url[:200]}"; raise AgentError(detail).
        Step 5: portal_fn = DISPATCH_TABLE[ats_info.ats_type]. Call portal_fn(job_id=job_id, model=model, event_callback=event_callback). Return result.
        On portal_fn raising AgentError: log pipeline_events entry if not already written; re-raise.

    Modify run_agents.py:
    - Add argparse argument: parser.add_argument("--auto-apply", action="store_true", default=False, help="Automatically submit the tailored resume to the detected ATS portal after QA approval. Default: OFF.")
    - Pass auto_apply=args.auto_apply to orchestrator.run().

    Modify agents/orchestrator.py:
    - Add auto_apply: bool = False parameter to run() function signature (after existing params).
    - After `_notify_success(payload, event_callback)` succeeds (i.e., after step 8 Confirmation Agent returns payload), add conditional block:
        if auto_apply:
            _notify(event_callback, "  [5/5] Running Portal Router (auto-apply)...", {...event...})
            from agents.portal_router import run as portal_run
            try:
                portal_result = _spawn_with_fallback(
                    conn, job_id, "portal_router", portal_run,
                    {"job_id": job_id},
                    event_callback,
                )
                _notify_auto_apply_success(portal_result, event_callback)
            except Exception as e:
                _handle_agent_error(conn, job_id, "portal_router", "submitting", str(e), event_callback)
    - Add _notify_auto_apply_success(result, event_callback) helper that prints outcome, ats_type, and ats_application_id.
    - CRITICAL — Add "portal_router" to _DEFAULT_AGENT_MODELS dict with value "google/gemini-3.1-flash-lite". The _spawn_with_fallback function does AGENT_MODELS[agent_name] with no guard — a missing key raises KeyError at runtime. This MUST be done before any call to _spawn_with_fallback with agent_name="portal_router". Following the existing pattern: _DEFAULT_AGENT_MODELS["portal_router"] = os.getenv("MODEL_PORTAL_ROUTER", "google/gemini-3.1-flash-lite"). The AGENT_MODELS dict is rebuilt from _DEFAULT_AGENT_MODELS via dict comprehension — adding to _DEFAULT_AGENT_MODELS is sufficient; no separate AGENT_MODELS line needed.

    Modify agents/__init__.py:
    - Add: from agents.portal_router import run as run_portal_router
    - Add "run_portal_router" to __all__.

    Run all 8 portal_router tests plus full suite regression check. Commit: "feat(18-06): implement portal_router, orchestrator wiring, --auto-apply flag"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/test_portal_router.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 portal_router tests pass. --auto-apply flag is accepted by run_agents.py (test: python run_agents.py --help | grep auto-apply). Full test suite passes without regression.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression and smoke-test</name>
  <files></files>
  <read_first>
    - tests/ directory listing (all test files created in this phase)
  </read_first>
  <action>
    Run the complete pytest suite to confirm all Phase 18 tests pass and no existing tests are broken.
    Then run a CLI smoke-test: python run_agents.py --help — confirm --auto-apply appears in the help output.
    Then run: python -c "from agents import run_portal_router; print('OK')" — confirm __init__.py export works.
    Fix any import errors or test failures before committing.
    No additional commit needed if all tests pass — the previous commits in this plan cover the changes.
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/ -v --tb=short 2>&1 | tail -30 && uv run python run_agents.py --help | grep -i "auto-apply"</automated>
  </verify>
  <done>Full test suite passes (no regressions). --auto-apply appears in CLI help output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job_url from DB → detect_ats regex | URL from DB (originally from user CLI/API) passed to regex; no code execution |
| --auto-apply CLI flag → orchestrator | User opt-in; default is OFF; no env var override (per D-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | Elevation of Privilege | --auto-apply flag submitting without user awareness | mitigate | default=False; require explicit --auto-apply per invocation; no "auto_apply_enabled" in user_profile (deferred per CONTEXT) |
| T-18-06-02 | Tampering | ats_type written to DB from detect_ats | accept | detect_ats returns constrained enum value (greenhouse/lever/ashby/unknown); no user-controlled value path |
| T-18-06-03 | Information Disclosure | unsupported ATS URL logged to pipeline_events | mitigate | truncate job_url to first 200 chars in pipeline_events detail |
| T-18-06-SC | Tampering | No new package installs | accept | all changes are to existing Python files |
</threat_model>

<verification>
1. uv run pytest tests/test_portal_router.py -v — all 8 tests pass
2. uv run pytest tests/ --tb=short — full suite passes, no regressions
3. uv run python run_agents.py --help | grep "auto-apply" — flag appears in help
4. python -c "from agents import run_portal_router; print('OK')"
5. python -c "from agents.orchestrator import run; import inspect; sig = inspect.signature(run); assert 'auto_apply' in sig.parameters; print('OK')"
</verification>

<success_criteria>
- Portal router dispatches to correct portal agent based on ats_type
- Unsupported ATS → submission_failed + pipeline_events with human-readable reason + no portal agent called
- --auto-apply flag gates the entire portal flow; omitting it means zero portal agent calls
- orchestrator.run() accepts and threads auto_apply to portal_router via _spawn_with_fallback
- No regressions in existing test suite
- 8 portal_router tests pass
</success_criteria>

<output>
Create .planning/phases/18-automatic-application-submission/18-06-SUMMARY.md when done
</output>
