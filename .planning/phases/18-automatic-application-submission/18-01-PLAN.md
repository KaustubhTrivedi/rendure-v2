---
phase: 18-automatic-application-submission
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - database/010_application_submissions.sql
  - utils/ats_detect.py
  - tests/utils/__init__.py
  - tests/utils/test_ats_detect.py
autonomous: true
requirements:
  - ATS-DETECT-01
  - DB-SCHEMA-01
must_haves:
  truths:
    - "Migration 010 is idempotent and adds ats_type, ats_board_token, ats_posting_id to jobs without breaking existing columns"
    - "Migration 010 creates application_submissions table with all required fields"
    - "Migration 010 inserts three new allowed_transitions rows: approved→submitting, submitting→submitted, submitting→submission_failed"
    - "detect_ats('https://boards.greenhouse.io/acme/jobs/12345') returns ats_type='greenhouse', board_token='acme', posting_id='12345'"
    - "detect_ats('https://jobs.lever.co/acme/abc-uuid') returns ats_type='lever', board_token='acme', posting_id='abc-uuid'"
    - "detect_ats('https://jobs.ashbyhq.com/acme/posting-id') returns ats_type='ashby', board_token='acme', posting_id='posting-id'"
    - "detect_ats for an unrecognized URL returns ats_type='unknown', board_token=None, posting_id=None"
  artifacts:
    - path: "database/010_application_submissions.sql"
      provides: "DB migration for ATS columns, application_submissions table, new allowed_transitions"
      contains: "CREATE TABLE IF NOT EXISTS application_submissions"
    - path: "utils/ats_detect.py"
      provides: "detect_ats() function"
      exports: ["detect_ats"]
    - path: "tests/utils/test_ats_detect.py"
      provides: "TDD tests for detect_ats"
      contains: "def test_detect_ats"
  key_links:
    - from: "utils/ats_detect.py"
      to: "database/010_application_submissions.sql"
      via: "detect_ats returns ats_type that matches allowed values in jobs.ats_type TEXT column"
      pattern: "greenhouse|lever|ashby|unknown"
---

<objective>
Create the DB migration and ATS detection utility that underpin all portal agents.

Purpose: Portal agents depend on (a) the application_submissions table and new allowed_transitions being present and (b) a reliable way to derive ATS type + API tokens from job_url. Both must exist before any portal agent can run.

Output:
- database/010_application_submissions.sql — additive migration, idempotent
- utils/ats_detect.py — detect_ats(url) → ATSInfo dataclass
- tests/utils/test_ats_detect.py — TDD tests (RED first, then GREEN)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-automatic-application-submission/18-CONTEXT.md
@database/schema.sql
@database/009_career_vault.sql
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for detect_ats()</name>
  <files>tests/utils/__init__.py, tests/utils/test_ats_detect.py</files>
  <read_first>
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (decisions D-01 through D-08, ATS URL patterns)
    - tests/__init__.py (existing pattern for test package init)
  </read_first>
  <behavior>
    - test_detect_ats_returns_greenhouse_for_boards_url: detect_ats("https://boards.greenhouse.io/acme/jobs/12345") == ATSInfo(ats_type="greenhouse", board_token="acme", posting_id="12345")
    - test_detect_ats_returns_greenhouse_for_job_boards_url: detect_ats("https://job-boards.greenhouse.io/token/jobs/999") == ATSInfo(ats_type="greenhouse", board_token="token", posting_id="999")
    - test_detect_ats_returns_greenhouse_board_only: detect_ats("https://boards.greenhouse.io/acme") returns ats_type="greenhouse", board_token="acme", posting_id=None
    - test_detect_ats_returns_lever: detect_ats("https://jobs.lever.co/myco/abc-123-def") == ATSInfo(ats_type="lever", board_token="myco", posting_id="abc-123-def")
    - test_detect_ats_returns_ashby: detect_ats("https://jobs.ashbyhq.com/startup/posting-uuid") == ATSInfo(ats_type="ashby", board_token="startup", posting_id="posting-uuid")
    - test_detect_ats_returns_unknown_for_linkedin: detect_ats("https://www.linkedin.com/jobs/view/123") returns ats_type="unknown", board_token=None, posting_id=None
    - test_detect_ats_returns_unknown_for_empty_string: detect_ats("") returns ats_type="unknown", board_token=None, posting_id=None
    - test_detect_ats_returns_unknown_for_workday: detect_ats("https://myco.wd1.myworkdayjobs.com/en-US/jobs") returns ats_type="unknown"
  </behavior>
  <action>
    Create tests/utils/__init__.py as an empty file (mirrors tests/__init__.py pattern).

    Create tests/utils/test_ats_detect.py. Import ATSInfo and detect_ats from utils.ats_detect. Write all behavior tests listed above. Do NOT import or implement utils/ats_detect.py yet — the tests must fail at import time (ImportError) to satisfy RED. Run pytest tests/utils/test_ats_detect.py and confirm failure.

    Commit with message: "test(18-01): add failing tests for detect_ats()"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/utils/test_ats_detect.py --tb=short 2>&1 | grep -E "ERROR|FAILED|ImportError" | head -5</automated>
  </verify>
  <done>pytest reports ImportError or ModuleNotFoundError for utils.ats_detect — tests are RED.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement detect_ats() and migration 010</name>
  <files>utils/ats_detect.py, database/010_application_submissions.sql</files>
  <read_first>
    - tests/utils/test_ats_detect.py (the failing tests — implement exactly what they test)
    - database/schema.sql (existing user_profile columns: email, phone, location, linkedin_url already present; jobs columns; allowed_transitions rows already present)
    - database/009_career_vault.sql (migration style and idempotent pattern to follow)
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (D-01 through D-11, ATS URL patterns, application_submissions schema)
  </read_first>
  <action>
    Create utils/ats_detect.py:
    - Define dataclass ATSInfo with fields: ats_type: str, board_token: str | None, posting_id: str | None.
    - Implement detect_ats(url: str) -> ATSInfo.
    - Compile three regex patterns at module level (not inside the function):
      GREENHOUSE_RE = re.compile(r"(?:boards|job-boards)\.greenhouse\.io/([^/?#]+)(?:/jobs/(\d+))?")
      LEVER_RE = re.compile(r"jobs\.lever\.co/([^/?#]+)/([^/?#]+)")
      ASHBY_RE = re.compile(r"jobs\.ashbyhq\.com/([^/?#]+)/([^/?#]+)")
    - Try each regex in order: greenhouse → lever → ashby. On first match, return ATSInfo with the captured groups. If no match, return ATSInfo(ats_type="unknown", board_token=None, posting_id=None).
    - No external dependencies. stdlib only (re, dataclasses).

    Create database/010_application_submissions.sql:
    Header comment: "Migration 010: Application Submissions — ATS detection columns, submissions table, new status transitions".

    Block 1 — Add columns to jobs (use DO $$ BEGIN IF NOT EXISTS ... END $$; guard per each column):
      - ats_type TEXT
      - ats_board_token TEXT
      - ats_posting_id TEXT

    Block 2 — Add columns to user_profile that are MISSING from the existing schema (guard each with IF NOT EXISTS):
      - github_url TEXT
      - portfolio_url TEXT
      - salary_expectation TEXT
    NOTE: email, phone, location, linkedin_url, website_url are ALREADY present in schema.sql (confirmed in database/schema.sql lines 214-220). DO NOT add them again — duplicate columns cause migration errors.

    Block 3 — Create application_submissions table:
      CREATE TABLE IF NOT EXISTS application_submissions (
          submission_id   UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
          job_id          UUID NOT NULL REFERENCES jobs(job_id) ON DELETE CASCADE,
          version_id      UUID NOT NULL REFERENCES resume_versions(version_id) ON DELETE CASCADE,
          ats_type        TEXT NOT NULL,
          ats_application_id TEXT,
          status          TEXT NOT NULL CHECK (status IN ('submitted','failed','skipped')),
          error_detail    TEXT,
          submitted_at    TIMESTAMPTZ DEFAULT NOW(),
          metadata        JSONB
      );

    Block 4 — Insert new allowed_transitions (ON CONFLICT DO NOTHING):
      ('approved', 'submitting'),
      ('submitting', 'submitted'),
      ('submitting', 'submission_failed')

    After implementing both files, run the tests and confirm GREEN. Commit:
      "feat(18-01): implement detect_ats() and migration 010"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/utils/test_ats_detect.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 8 detect_ats tests pass. database/010_application_submissions.sql exists with application_submissions table definition and three new allowed_transitions rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job_url input → ATS regex | Untrusted URL from DB; regex must not expand to arbitrary code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | Tampering | detect_ats regex on job_url | accept | URL is already stored in DB from a previous validated step; regex is read-only pattern match, no exec |
| T-18-01-02 | Information Disclosure | application_submissions.error_detail | mitigate | error_detail must contain only structured error strings; never log full HTTP response bodies containing auth headers |
| T-18-01-SC | Tampering | No new npm/pip installs in this plan | accept | No new packages installed; migration is raw SQL |
</threat_model>

<verification>
1. uv run pytest tests/utils/test_ats_detect.py -v — all 8 tests pass
2. grep "application_submissions" database/010_application_submissions.sql returns the CREATE TABLE statement
3. grep "approved.*submitting\|submitting.*submitted\|submitting.*submission_failed" database/010_application_submissions.sql returns 3 lines
4. python -c "from utils.ats_detect import detect_ats; r = detect_ats('https://boards.greenhouse.io/acme/jobs/99'); assert r.ats_type == 'greenhouse'; print('OK')"
</verification>

<success_criteria>
- detect_ats correctly classifies Greenhouse, Lever, Ashby, and unknown URLs
- Migration 010 is idempotent (safe to run twice without error)
- All new DB columns/tables are additive — no existing columns removed or renamed
- 8 passing pytest tests in tests/utils/test_ats_detect.py
</success_criteria>

<output>
Create .planning/phases/18-automatic-application-submission/18-01-SUMMARY.md when done
</output>
