---
phase: 18-automatic-application-submission
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - answers.yaml
  - utils/answer_engine.py
  - utils/resume_render.py
  - tests/utils/test_answer_engine.py
  - tests/utils/test_resume_render.py
autonomous: true
requirements:
  - ANSWER-ENGINE-01
  - RESUME-RENDER-01
must_haves:
  truths:
    - "AnswerEngine.lookup('Are you authorized to work in the United States?') returns the stock answer when answers.yaml key 'authorized to work' is present (per D-04 fuzzy substring match)"
    - "AnswerEngine.lookup falls back to LLM when no key matches, passing persona_context and resume content to the prompt (per D-05)"
    - "render_resume_to_pdf raises EnvironmentError with install instructions when rendercv is not on PATH (per D-10)"
    - "render_resume_to_pdf calls subprocess.run with rendercv render and the temp YAML path, deletes the temp file after use (per D-11)"
    - "render_resume_to_pdf returns a bytes object on success"
    - "answers.yaml ships at project root with pre-populated stock_answers dict and persona_context block"
  artifacts:
    - path: "answers.yaml"
      provides: "Stock screening answer template with persona_context"
      contains: "stock_answers"
    - path: "utils/answer_engine.py"
      provides: "AnswerEngine class with lookup() method"
      exports: ["AnswerEngine"]
    - path: "utils/resume_render.py"
      provides: "render_resume_to_pdf() function"
      exports: ["render_resume_to_pdf"]
    - path: "tests/utils/test_answer_engine.py"
      provides: "TDD tests for AnswerEngine"
    - path: "tests/utils/test_resume_render.py"
      provides: "TDD tests for render_resume_to_pdf"
  key_links:
    - from: "utils/answer_engine.py"
      to: "answers.yaml"
      via: "AnswerEngine reads answers.yaml from PROJECT_ROOT/answers.yaml at instantiation"
      pattern: "answers.yaml"
    - from: "utils/resume_render.py"
      to: "shutil.which('rendercv')"
      via: "checks PATH for rendercv binary before calling subprocess"
      pattern: "shutil.which"
---

<objective>
Implement the answer engine (screening question lookup + LLM fallback) and PDF rendering utility, parallel to the DB/ATS plan.

Purpose: Portal agents need to answer screening questions before POSTing applications, and need to render YAML → PDF immediately before upload. Both are utility modules with no inter-dependency, so they run in Wave 1 alongside plan 18-01.

Output:
- answers.yaml — pre-populated template
- utils/answer_engine.py — AnswerEngine class
- utils/resume_render.py — render_resume_to_pdf() function
- tests for both utilities (TDD: RED then GREEN)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-automatic-application-submission/18-CONTEXT.md
@utils/llm.py
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for AnswerEngine and render_resume_to_pdf</name>
  <files>tests/utils/test_answer_engine.py, tests/utils/test_resume_render.py</files>
  <read_first>
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (D-03 through D-06 for answer engine; D-09 through D-11 for PDF rendering)
    - utils/llm.py (load_llm signature — mock target for LLM fallback tests)
    - tests/__init__.py (existing test package init pattern)
  </read_first>
  <behavior>
    AnswerEngine tests (tests/utils/test_answer_engine.py):
    - test_answer_engine_returns_stock_answer_on_exact_key_substring_match: with answers.yaml containing key "authorized to work" mapped to "Yes", AnswerEngine.lookup("Are you authorized to work in the United States?") returns "Yes" without calling LLM
    - test_answer_engine_match_is_case_insensitive: key "Salary Expectations" matches question "what are your salary expectations?" 
    - test_answer_engine_returns_first_matching_key_when_multiple_partial_matches: deterministic — first key in dict order wins
    - test_answer_engine_falls_back_to_llm_when_no_key_matches: mock load_llm; assert llm.invoke called once when no key matches; return value is llm.invoke's return value
    - test_answer_engine_llm_prompt_includes_persona_context: the prompt passed to llm.invoke contains the persona_context string from answers.yaml
    - test_answer_engine_llm_prompt_includes_resume_content: the prompt passed to llm.invoke contains the resume_content passed to lookup()
    - test_answer_engine_raises_on_missing_answers_yaml: AnswerEngine(path="/nonexistent/answers.yaml") raises FileNotFoundError

    render_resume_to_pdf tests (tests/utils/test_resume_render.py):
    - test_render_resume_raises_environment_error_when_rendercv_missing: patch shutil.which to return None; render_resume_to_pdf("yaml content", tmp_path) raises EnvironmentError containing "rendercv" (the error message should explain how to install it, without prescribing a specific package manager command since rendercv[full] is already in pyproject.toml)
    - test_render_resume_calls_subprocess_with_correct_args: patch shutil.which to return "/usr/local/bin/rendercv"; patch subprocess.run to return CompletedProcess(returncode=0); assert subprocess.run called with ["rendercv", "render", <yaml_path>] and that the temp YAML file is deleted after the call
    - test_render_resume_returns_pdf_bytes_on_success: subprocess.run returns 0; a fake PDF is written to the expected output path; assert render_resume_to_pdf returns bytes
    - test_render_resume_raises_on_nonzero_exit: subprocess.run returns returncode=1 with stderr="error msg"; raises RuntimeError containing "rendercv" and "error msg"
    - test_render_resume_deletes_temp_yaml_on_failure: even when subprocess.run raises RuntimeError, the temp YAML file must be deleted (test with assert not path.exists() in finally block)
  </behavior>
  <action>
    Create tests/utils/test_answer_engine.py. Import AnswerEngine from utils.answer_engine. Use unittest.mock.patch and tmp_path fixture to create temporary answers.yaml files. Write all 7 behavior tests above. Do NOT implement utils/answer_engine.py yet.

    Create tests/utils/test_resume_render.py. Import render_resume_to_pdf from utils.resume_render. Use unittest.mock.patch for shutil.which and subprocess.run. Write all 5 behavior tests above.

    Run pytest tests/utils/test_answer_engine.py tests/utils/test_resume_render.py and confirm ImportError/ModuleNotFoundError — tests are RED.

    Commit: "test(18-02): add failing tests for AnswerEngine and render_resume_to_pdf"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/utils/test_answer_engine.py tests/utils/test_resume_render.py --tb=short 2>&1 | grep -E "ERROR|FAILED|ImportError|ModuleNotFound" | head -5</automated>
  </verify>
  <done>pytest reports ImportError or ModuleNotFoundError — all tests are RED.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement AnswerEngine, render_resume_to_pdf, and answers.yaml</name>
  <files>utils/answer_engine.py, utils/resume_render.py, answers.yaml</files>
  <read_first>
    - tests/utils/test_answer_engine.py (the failing tests — implement exactly what they test)
    - tests/utils/test_resume_render.py (the failing tests — implement exactly what they test)
    - utils/llm.py (load_llm signature, extract_json usage pattern)
    - .planning/phases/18-automatic-application-submission/18-CONTEXT.md (D-03 through D-11)
  </read_first>
  <action>
    Create answers.yaml at project root. Structure:
      stock_answers:
        "authorized to work": "Yes"
        "sponsorship": "No sponsorship required"
        "salary expectations": "Negotiable based on the full compensation package"
        "years of experience": "Please see my resume for details"
        "remote work": "Yes, I am comfortable with remote work"
        "start date": "I can start within two weeks of receiving an offer"
        "willing to relocate": "Open to discussing relocation"
        "bachelor's degree": "Yes, I hold a relevant degree"
        "criminal background": "No"
        "drug test": "Yes, I consent to a drug screening"
      persona_context: |
        Answer screening questions professionally and concisely. If a question cannot be answered
        without fabricating information (specific salary numbers, exact dates), reply with "N/A"
        or ask the recruiter for clarification. Never fabricate credentials, employment history,
        or certifications. Tone: confident, direct, and professional.

    Create utils/answer_engine.py:
    - Class AnswerEngine with __init__(self, path: str | Path = PROJECT_ROOT / "answers.yaml", llm=None, model_name: str | None = None).
      - Load YAML at init. Raise FileNotFoundError if path does not exist.
      - Store stock_answers dict and persona_context string.
      - Accept optional llm instance (for testing injection); if None, create lazily on first LLM call via load_llm(model_name=model_name, temperature=0.1, max_tokens=512).
    - Method lookup(self, question: str, resume_content: str = "", jd_text: str = "") -> str:
      - Normalize question to lowercase.
      - Iterate stock_answers keys; normalize each key to lowercase; if key is a substring of the normalized question, return the mapped value immediately (D-04).
      - On no match: build a prompt string containing the persona_context, question, resume_content (first 3000 chars), and jd_text (first 2000 chars). Call llm.invoke(prompt). Return stripped response.
    - PROJECT_ROOT = Path(__file__).resolve().parent.parent

    Create utils/resume_render.py:
    - Function render_resume_to_pdf(yaml_content: str, tmp_dir: Path | str | None = None) -> bytes:
      - Check shutil.which("rendercv"); if None, raise EnvironmentError with message:
        "rendercv not found on PATH. Ensure the project virtualenv is active — rendercv[full] is already in pyproject.toml and should be at .venv/bin/rendercv."
        NOTE: rendercv[full]>=2.8 is already in pyproject.toml; do NOT add it as a new dependency. The check exists for environments where .venv is not activated.
      - Write yaml_content to a temp .yaml file in tmp_dir (or tempfile.mkdtemp() if None).
      - Call subprocess.run(["rendercv", "render", str(yaml_path)], capture_output=True, text=True, timeout=120).
      - On returncode != 0: delete temp YAML; raise RuntimeError(f"rendercv failed: {result.stderr[:500]}").
      - Locate the rendered PDF (rendercv outputs to a subdirectory; discover the .pdf file via glob on the temp dir).
      - Read PDF bytes. Delete temp YAML file (and temp dir if created here). Return PDF bytes.
    - All temp file cleanup must occur in finally blocks so tests for failure-path deletion pass.

    Run the full test suite and confirm all 12 tests pass. Commit:
      "feat(18-02): implement AnswerEngine, render_resume_to_pdf, and answers.yaml template"
  </action>
  <verify>
    <automated>cd /Users/kaustubhtrivedi/Projects/jobs-tracker && uv run pytest tests/utils/test_answer_engine.py tests/utils/test_resume_render.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 tests (7 AnswerEngine + 5 resume_render) pass. answers.yaml exists at project root with stock_answers and persona_context keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| answers.yaml → AnswerEngine | File on disk; user-controlled content passed to LLM prompt |
| resume_versions.latex_source → subprocess | DB content passed to rendercv CLI via temp file on disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | answers.yaml content injected into LLM prompt | accept | persona_context is user-controlled by design; no external attacker can modify it without filesystem access |
| T-18-02-02 | Information Disclosure | resume YAML written to temp file | mitigate | use tempfile.mkdtemp() with mode=0o700; delete immediately after subprocess call in finally block |
| T-18-02-03 | Denial of Service | rendercv subprocess timeout | mitigate | subprocess.run timeout=120 raises TimeoutExpired; caller sets job to submission_failed |
| T-18-02-SC | Tampering | No new npm/pip installs in this plan | accept | yaml and subprocess are stdlib; load_llm already in codebase |
</threat_model>

<verification>
1. uv run pytest tests/utils/test_answer_engine.py tests/utils/test_resume_render.py -v — 12 tests pass
2. python -c "from utils.answer_engine import AnswerEngine; print('OK')"
3. python -c "from utils.resume_render import render_resume_to_pdf; print('OK')"
4. grep "stock_answers" answers.yaml and grep "persona_context" answers.yaml both return matches
</verification>

<success_criteria>
- AnswerEngine.lookup does substring match, case-insensitive, with LLM fallback
- render_resume_to_pdf raises EnvironmentError when rendercv absent; cleans up temp files on both success and failure
- answers.yaml ships with at least 10 stock answers covering common screening questions
- 12 passing pytest tests
</success_criteria>

<output>
Create .planning/phases/18-automatic-application-submission/18-02-SUMMARY.md when done
</output>
