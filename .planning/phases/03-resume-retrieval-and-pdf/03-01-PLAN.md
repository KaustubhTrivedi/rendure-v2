---
phase: 03-resume-retrieval-and-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/routes/jobs.ts
  - api/src/routes/jobs.test.ts
autonomous: true
requirements:
  - RESUME-01
  - RESUME-04
  - RESUME-05
must_haves:
  truths:
    - "A client with a valid X-API-Key can list every resume_versions row for a job."
    - "A client with a valid X-API-Key can fetch raw tailored source text for a version that belongs to the job."
    - "Unknown jobs, unknown versions, and cross-job version IDs return the same 404 problem JSON shape."
  artifacts:
    - path: "api/src/routes/jobs.ts"
      provides: "GET /jobs/:id/resumes and GET /jobs/:id/resume/:version_id route handlers"
      contains: "WHERE job_id = $1 AND version_id = $2"
    - path: "api/src/routes/jobs.test.ts"
      provides: "Vitest coverage for auth inheritance, list, raw source, and 404 behavior"
      contains: "returns 401 without X-API-Key for resume list when routed through the mounted app"
  key_links:
    - from: "api/src/index.ts"
      to: "api/src/routes/jobs.ts"
      via: "app.use('/jobs/*', apiKeyMiddleware()) before app.route('/jobs', jobs)"
      pattern: "app\\.use\\('/jobs/\\*', apiKeyMiddleware\\(\\)\\)"
    - from: "api/src/routes/jobs.ts"
      to: "resume_versions"
      via: "parameterized pool.query calls"
      pattern: "FROM resume_versions[\\s\\S]*WHERE job_id = \\$1"
---

<objective>
Add the read-only resume metadata and raw source endpoint under the existing `/jobs` route group.

Purpose: Clients need authenticated access to stored resume iterations before PDF rendering is useful.
Output: `GET /jobs/:id/resumes` and `GET /jobs/:id/resume/:version_id` with tests proving auth inheritance, parameterized SQL, D-17 `text/markdown` content type, and uniform 404s. Current stored source is RenderCV YAML returned as stored text; the endpoint keeps the `text/markdown` content type for the locked backward-compatible contract.
</objective>

<execution_context>
@/Users/kaustubhtrivedi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kaustubhtrivedi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-resume-retrieval-and-pdf/03-CONTEXT.md
@.planning/phases/03-resume-retrieval-and-pdf/03-RESEARCH.md
@.planning/phases/03-resume-retrieval-and-pdf/03-VALIDATION.md
@api/src/index.ts
@api/src/routes/jobs.ts
@api/src/routes/jobs.test.ts
@api/src/errors.ts
@api/src/db.ts

<interfaces>
From `api/src/index.ts`:
```ts
app.use('/jobs/*', apiKeyMiddleware())
app.route('/jobs', jobs)
```

From `api/src/errors.ts`:
```ts
export function httpError(c: Context, status: number, code: ErrorCodeValue, title: string, opts?: HttpErrorOptions)
```

From `api/src/db.ts`:
```ts
export const pool = new Pool({ connectionString: process.env.DATABASE_URL })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resume version list endpoint</name>
  <files>api/src/routes/jobs.ts, api/src/routes/jobs.test.ts</files>
  <read_first>
    api/src/index.ts
    api/src/routes/jobs.ts
    api/src/routes/jobs.test.ts
    api/src/errors.ts
    .planning/phases/03-resume-retrieval-and-pdf/03-CONTEXT.md
  </read_first>
  <behavior>
    - RED: Add one failing mounted-app auth test named `returns 401 without X-API-Key for resume list when routed through the mounted app`; request `app.request('/jobs/job-123/resumes')` and expect HTTP 401 with `code: 'unauthorized'`.
    - GREEN: No per-route auth code; rely on `app.use('/jobs/*', apiKeyMiddleware())`.
    - RED: Add one failing route test named `lists all resume versions for a job ordered by version_number`; mock `pool.query` and request `jobs.request('/job-123/resumes')`.
    - GREEN: Implement `GET /:id/resumes` before `GET /:id/status` and `GET /:id`. Return JSON array only, per D-09 and D-19.
    - RED: Add one failing route test named `returns 404 when listing resumes for an unknown job`; mock no resume rows and expect `code: 'not_found'`.
  </behavior>
  <action>
    Implement `GET /jobs/:id/resumes` in `api/src/routes/jobs.ts` as `jobs.get('/:id/resumes', async (c) => ...)`.
    Use exactly one parameterized query:
    `SELECT version_id, version_number, created_at, tailoring_notes FROM resume_versions WHERE job_id = $1 ORDER BY version_number ASC`
    with params `[id]`.
    If `result.rows.length === 0`, return `httpError(c, 404, 'not_found', 'Job not found.')` per D-12. Do not add QA score, `is_active`, envelopes, writes, agents, or schema changes.
  </action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/routes/jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jobs.get('/:id/resumes'" api/src/routes/jobs.ts` finds the route before `jobs.get('/:id/status'`.
    - `grep -n "SELECT version_id, version_number, created_at, tailoring_notes" api/src/routes/jobs.ts` finds the list projection.
    - `grep -n "WHERE job_id = \\$1" api/src/routes/jobs.ts` finds the parameterized job filter.
    - `cd api &amp;&amp; npm test -- src/routes/jobs.test.ts` passes.
  </acceptance_criteria>
  <done>`GET /jobs/:id/resumes` returns `200` with `version_id`, `version_number`, `created_at`, and `tailoring_notes` for all versions of a job, and returns `404` with `code: 'not_found'` when no versions exist for that job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tailored Markdown endpoint with cross-job protection</name>
  <files>api/src/routes/jobs.ts, api/src/routes/jobs.test.ts</files>
  <read_first>
    api/src/routes/jobs.ts
    api/src/routes/jobs.test.ts
    api/src/errors.ts
    database/schema.sql
    .planning/phases/03-resume-retrieval-and-pdf/03-CONTEXT.md
  </read_first>
  <behavior>
    - RED: Add one failing test named `returns tailored source with text markdown content type`; mock one row `{ latex_source: 'cv:\n  name: Test\ndesign:\n  theme: sb2nov\n' }`, request `jobs.request('/job-123/resume/ver-123')`, expect status 200, that exact body, and `content-type` containing `text/markdown; charset=utf-8`.
    - GREEN: Implement the raw source route with raw `latex_source` body. Per D-17 keep `text/markdown; charset=utf-8`; current `latex_source` content is RenderCV YAML returned as stored text.
    - RED: Add one failing test named `returns uniform 404 when resume version is missing or belongs to another job`; mock no rows for `WHERE job_id = $1 AND version_id = $2`, expect `code: 'not_found'` and no distinct cross-job message.
    - GREEN: Use the exact two-column ownership predicate from D-11.
  </behavior>
  <action>
    Implement `GET /jobs/:id/resume/:version_id` in `api/src/routes/jobs.ts` as `jobs.get('/:id/resume/:version_id', async (c) => ...)`.
    Query exactly:
    `SELECT latex_source FROM resume_versions WHERE job_id = $1 AND version_id = $2`
    with params `[id, versionId]`.
    On no rows, return `httpError(c, 404, 'not_found', 'Resume version not found.')`.
    On success, return the raw `latex_source` with header `Content-Type: text/markdown; charset=utf-8` per D-17. Current rows are RenderCV YAML produced by `agents/resume_tailor.py`, but this endpoint intentionally returns the stored text unchanged for backward-compatible naming/content contract. Do not gate on `jobs.status`, `jobs.active_resume_id`, or approval status per D-10.
  </action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/routes/jobs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jobs.get('/:id/resume/:version_id'" api/src/routes/jobs.ts` finds the Markdown route before `jobs.get('/:id/status'`.
    - `grep -n "WHERE job_id = \\$1 AND version_id = \\$2" api/src/routes/jobs.ts` finds the cross-job ownership predicate.
    - `grep -n "text/markdown; charset=utf-8" api/src/routes/jobs.ts api/src/routes/jobs.test.ts` finds the content type in implementation and tests.
    - `grep -n "latex_source" api/src/routes/jobs.ts` finds the legacy column name preserved.
    - `cd api &amp;&amp; npm test -- src/routes/jobs.test.ts` passes.
  </acceptance_criteria>
  <done>`GET /jobs/:id/resume/:version_id` returns raw tailored source text only when the version belongs to the requested job, with uniform 404 behavior for missing/cross-job IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Hono `/jobs/:id/resumes` | Untrusted route `:id` reaches SQL query parameters. |
| client→Hono `/jobs/:id/resume/:version_id` | Untrusted route `:id` and `:version_id` reach SQL query parameters and ownership checks. |
| Hono→PostgreSQL | API reads immutable pipeline tables through `pool.query`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `jobs.ts` resume SQL queries | mitigate | Use `pool.query(sql, [id])` and `pool.query(sql, [id, versionId])`; no string interpolation. |
| T-03-02 | Information Disclosure | Markdown version lookup | mitigate | Query `resume_versions` with `WHERE job_id = $1 AND version_id = $2` and return uniform 404. |
| T-03-03 | Tampering | Filesystem cache | accept | No filesystem cache code in this plan; covered in Plan 02/03. |
| T-03-04 | Information Disclosure | Render stderr/source | accept | No RenderCV subprocess in this plan; covered in Plan 02/03. |
| T-03-05 | Denial of Service | Render concurrency | accept | No RenderCV subprocess in this plan; covered in Plan 02/03. |
</threat_model>

<verification>
Run `cd api &amp;&amp; npm test -- src/routes/jobs.test.ts`.
Run `cd api &amp;&amp; npm run build` after this plan if it is executed alone.
</verification>

<success_criteria>
- `GET /jobs/:id/resumes` returns the full version metadata list for a job.
- `GET /jobs/:id/resume/:version_id` returns raw stored source text with `Content-Type: text/markdown; charset=utf-8`; current stored source is RenderCV YAML.
- Both endpoints use parameterized SQL only.
- Cross-job version IDs return 404 and do not leak existence under another job.
- Auth inheritance through `app` is tested for the new resume route group.
</success_criteria>

<output>
After completion, create `.planning/phases/03-resume-retrieval-and-pdf/03-01-SUMMARY.md`.
</output>
