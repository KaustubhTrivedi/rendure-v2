---
phase: 04-telegram-bot-integration
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - api/src/telegram-notifier.ts
  - api/src/telegram-notifier.test.ts
  - api/src/index.ts
  - README.md
autonomous: true
requirements: [TELEGRAM-03, TELEGRAM-04, TELEGRAM-05]
user_setup:
  - service: telegram
    why: "Terminal-state notifications"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather token"
      - name: TELEGRAM_WEBHOOK_SECRET
        source: "Secret configured in Telegram setWebhook"
    dashboard_config:
      - task: "Set notify_telegram_chat_id through PATCH /profile; null disables notifications"
        location: "Rendure API /profile"
must_haves:
  truths:
    - "When a job reaches approved, low_match, or error, a Telegram message is sent to user_profile.notify_telegram_chat_id if configured."
    - "If notify_telegram_chat_id is null, no Telegram message is sent."
    - "Approved messages include status, QA score, role/company when available, and resume retrieval API paths."
    - "Low-match messages include concise high-severity QA gaps when available."
    - "Error messages are safe and plain."
    - "Server startup does not crash when Telegram env vars are missing."
  artifacts:
    - path: "api/src/telegram-notifier.ts"
      provides: "Pipeline terminal event listener and notification dispatcher"
      exports: ["startTelegramTerminalNotifier", "notifyTerminalJob"]
    - path: "api/src/telegram-notifier.test.ts"
      provides: "Notifier listener/query/send tests"
    - path: "api/src/index.ts"
      provides: "Non-fatal notifier startup wiring"
    - path: "README.md"
      provides: "Telegram configuration and webhook usage docs"
  key_links:
    - from: "api/src/telegram-notifier.ts"
      to: "api/src/pg-listener.ts"
      via: "listenForPipelineEvents notification wake-up"
      pattern: "listenForPipelineEvents"
    - from: "api/src/telegram-notifier.ts"
      to: "api/src/telegram.ts"
      via: "formatTelegramTerminalMessage and sendTelegramMessage"
      pattern: "sendTelegramMessage"
    - from: "api/src/index.ts"
      to: "api/src/telegram-notifier.ts"
      via: "startTelegramTerminalNotifier on non-test startup"
      pattern: "startTelegramTerminalNotifier"
---

<objective>
Wire terminal pipeline events to outbound Telegram notifications.

Purpose: The backend must notify the configured Telegram chat when pipeline work finishes, while respecting `notify_telegram_chat_id = null` and missing Telegram configuration.
Output: Terminal notifier, startup wiring, tests, and docs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-telegram-bot-integration/04-CONTEXT.md
@.planning/phases/04-telegram-bot-integration/04-RESEARCH.md
@.planning/phases/04-telegram-bot-integration/04-02-SUMMARY.md
@.planning/phases/04-telegram-bot-integration/04-03-SUMMARY.md
@api/src/pg-listener.ts
@api/src/sse.ts
@api/src/telegram.ts
@api/src/index.ts
@database/002_telegram.sql
@database/003_pipeline_events_notify.sql

<interfaces>
From `api/src/pg-listener.ts`:
```typescript
export interface PipelineNotification { job_id: string; event_id: string }
export interface PipelineEventListener { close(): Promise<void> }
export async function listenForPipelineEvents(onEvent: (notification: PipelineNotification) => void): Promise<PipelineEventListener>
```

From `api/src/sse.ts`:
```typescript
export const TERMINAL_STATUSES = new Set(['approved', 'low_match', 'error'])
export function isTerminalStatus(status: string | null | undefined): boolean
```

From Plan 04-02:
```typescript
export function formatTelegramTerminalMessage(job: TelegramTerminalJob): string
export async function sendTelegramMessage(chatId: string, text: string): Promise<{ ok: true } | { ok: false; error: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement terminal job notification query and send</name>
  <files>api/src/telegram-notifier.ts, api/src/telegram-notifier.test.ts</files>
  <behavior>
    - RED: terminal approved job query reads job status/score/company/role/active_resume_id, latest QA gaps, and `user_profile.notify_telegram_chat_id`, then sends formatted Telegram message per D-09.
    - RED: low_match sends top high-severity gaps through formatter per D-10.
    - RED: error sends safe plain message per D-11.
    - RED: null `notify_telegram_chat_id` returns `{ sent: false, reason: "telegram_chat_not_configured" }` and does not call fetch per TELEGRAM-04.
    - RED: non-terminal job returns `{ sent: false, reason: "not_terminal" }`.
  </behavior>
  <action>Create `api/src/telegram-notifier.ts` exporting `notifyTerminalJob(jobId: string)`. Query canonical current state with parameterized SQL joining/reading `jobs`, single-row `user_profile`, and latest `qa_reviews` via `resume_versions` for gaps. Do not write to trigger-owned columns. Use `formatTelegramTerminalMessage` and `sendTelegramMessage`. Respect `notify_telegram_chat_id = null` exactly; do not infer chat ID from webhook updates.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/telegram-notifier.test.ts</automated>
  </verify>
  <done>`notifyTerminalJob` sends only for terminal jobs with configured chat ID and uses safe formatted messages.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: wire pg listener to terminal notifier</name>
  <files>api/src/telegram-notifier.ts, api/src/telegram-notifier.test.ts</files>
  <behavior>
    - RED: `startTelegramTerminalNotifier()` registers `listenForPipelineEvents` and calls `notifyTerminalJob(notification.job_id)` when a notification arrives.
    - RED: duplicate notifications for the same terminal job in the same process do not send duplicate Telegram messages.
    - RED: missing Telegram bot token makes startup a no-op listener result and does not crash per TELEGRAM-05.
    - RED: listener/query/send errors are caught and logged/surfaced as typed results without crashing server startup.
  </behavior>
  <action>Implement `startTelegramTerminalNotifier()` using `listenForPipelineEvents` as a wake-up mechanism. Re-query job state in `notifyTerminalJob`; do not trust notification payload beyond `job_id`. Add an in-memory sent-job set for process-local duplicate suppression. If Telegram is unconfigured, return a disabled notifier object without opening a DB listener. Keep errors non-fatal.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/telegram-notifier.test.ts</automated>
  </verify>
  <done>Notifier starts safely, reacts to pipeline event notifications, suppresses duplicate sends, and degrades cleanly when unconfigured.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: startup wiring and operational docs</name>
  <files>api/src/index.ts, README.md</files>
  <behavior>
    - RED: non-test startup invokes `startTelegramTerminalNotifier()` without preventing server startup if Telegram is unconfigured.
    - RED: README documents `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `/telegram/webhook`, `X-Telegram-Bot-Api-Secret-Token`, and `notify_telegram_chat_id = null` disables notifications.
  </behavior>
  <action>In `api/src/index.ts`, start the Telegram terminal notifier only outside `NODE_ENV=test`, similar to the RenderCV availability probe. Log warnings without exiting on missing Telegram config or listener errors. Update README with exact Telegram env vars, webhook setup, the API-key exception for `/telegram/webhook`, and profile chat ID behavior.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/telegram-notifier.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <done>Server startup remains non-fatal for Telegram config, notifier is wired, docs are accurate, and build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres notification→API listener | Notification wakes API process but canonical data must be re-read. |
| API→Telegram Bot API | Server sends terminal job data to external Telegram service. |
| Profile config→notification dispatch | `notify_telegram_chat_id` controls recipient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | I | api/src/telegram-notifier.ts | mitigate | Send only concise status/score/gaps/paths; no stack traces or raw exception details. |
| T-04-04-02 | S | api/src/telegram-notifier.ts | mitigate | Recipient comes only from `user_profile.notify_telegram_chat_id`, not incoming webhook chat IDs. |
| T-04-04-03 | T | api/src/telegram-notifier.ts | mitigate | Treat pg notification as wake-up only; re-query canonical job/profile/QA rows with parameterized SQL. |
| T-04-04-04 | D | api/src/telegram-notifier.ts | mitigate | Missing Telegram config and listener/send failures are non-fatal to API startup and routes. |
| T-04-04-05 | R | api/src/telegram-notifier.ts | accept | No durable notification audit table in Phase 4; use existing logs/typed testable results. |
</threat_model>

<verification>
Run `cd api &amp;&amp; npm test -- src/telegram.test.ts src/routes/telegram.test.ts src/telegram-notifier.test.ts src/app.test.ts`, `cd api &amp;&amp; npm test`, and `cd api &amp;&amp; npm run build`.
</verification>

<success_criteria>
Terminal notifications satisfy TELEGRAM-03, TELEGRAM-04, TELEGRAM-05 and D-09 through D-15, using existing pipeline event notifications and Phase 3 resume retrieval paths.
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-bot-integration/04-04-SUMMARY.md`
</output>
