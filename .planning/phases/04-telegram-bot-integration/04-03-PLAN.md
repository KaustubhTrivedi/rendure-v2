---
phase: 04-telegram-bot-integration
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - api/src/routes/telegram.ts
  - api/src/routes/telegram.test.ts
  - api/src/index.ts
  - api/src/app.test.ts
autonomous: true
requirements: [TELEGRAM-01, TELEGRAM-02, TELEGRAM-05]
user_setup:
  - service: telegram
    why: "Incoming Telegram webhook authentication"
    env_vars:
      - name: TELEGRAM_WEBHOOK_SECRET
        source: "Secret used when configuring Telegram setWebhook(secret_token)"
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather token; missing token makes Telegram routes return 503"
    dashboard_config:
      - task: "Configure Telegram webhook to POST to /telegram/webhook with the same secret_token"
        location: "Telegram Bot API setWebhook"
must_haves:
  truths:
    - "POST /telegram/webhook rejects missing/invalid secret token with 401 and creates no job."
    - "Missing Telegram configuration returns 503 without crashing the server."
    - "A valid Telegram text update with one job URL submits through shared job behavior and returns 202 with job/status fields."
    - "Duplicate URL submissions return the existing job/status behavior and do not spawn another pipeline."
    - "Non-URL messages and /start return friendly help with 200 so Telegram does not retry."
    - "The Telegram webhook route is mounted without requiring X-API-Key."
  artifacts:
    - path: "api/src/routes/telegram.ts"
      provides: "Telegram webhook receiver with route-specific secret auth and URL-only intake"
      exports: ["default"]
    - path: "api/src/routes/telegram.test.ts"
      provides: "Webhook route tests"
    - path: "api/src/index.ts"
      provides: "Mounts /telegram route group outside API-key middleware"
    - path: "api/src/app.test.ts"
      provides: "App-level mount/auth regression tests"
  key_links:
    - from: "api/src/index.ts"
      to: "api/src/routes/telegram.ts"
      via: "app.route('/telegram', telegram)"
      pattern: "route('/telegram'"
    - from: "api/src/routes/telegram.ts"
      to: "api/src/job-submission.ts"
      via: "submitJobUrl(url)"
      pattern: "submitJobUrl"
---

<objective>
Add the Telegram webhook receiver and mount it as a public Telegram-secret-authenticated route.

Purpose: Telegram users can submit job URLs through the same backend pipeline entry path while preserving API-key protection for normal API routes and exact Telegram secret validation for the webhook.
Output: `/telegram/webhook` route, tests, and app mount.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-telegram-bot-integration/04-CONTEXT.md
@.planning/phases/04-telegram-bot-integration/04-RESEARCH.md
@.planning/phases/04-telegram-bot-integration/04-01-SUMMARY.md
@api/src/index.ts
@api/src/errors.ts
@api/src/job-submission.ts
@api/src/app.test.ts

<interfaces>
From Plan 04-01:
```typescript
export function statusUrl(jobId: string): string
export async function submitJobUrl(url: string): Promise<SubmitJobResult>
```

Telegram update subset to support:
```typescript
type TelegramUpdate = {
  update_id?: number
  message?: {
    text?: string
    chat?: { id?: number | string }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement Telegram secret/config gate</name>
  <files>api/src/routes/telegram.ts, api/src/routes/telegram.test.ts</files>
  <behavior>
    - RED: missing `TELEGRAM_BOT_TOKEN` or `TELEGRAM_WEBHOOK_SECRET` returns 503 problem JSON with stable code/detail per D-07/TELEGRAM-05.
    - RED: missing or wrong `X-Telegram-Bot-Api-Secret-Token` returns 401 problem JSON and does not call `submitJobUrl` per D-05/TELEGRAM-02.
    - RED: exact header match proceeds without requiring `X-API-Key` per D-06.
  </behavior>
  <action>Create `api/src/routes/telegram.ts` with `POST /webhook`. Check configuration first, then exact header match against `process.env.TELEGRAM_WEBHOOK_SECRET`. Use `httpError` for 401 and 503. Do not mount `apiKeyMiddleware` here and do not modify `/jobs/*` or `/profile/*` auth. Compare the header as a full string equality; do not accept alternate headers or API-key fallback because D-05/D-06 lock the mechanism.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/routes/telegram.test.ts</automated>
  </verify>
  <done>Webhook rejects unconfigured and unauthenticated requests before parsing/submitting jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement URL-only Telegram update handling</name>
  <files>api/src/routes/telegram.ts, api/src/routes/telegram.test.ts</files>
  <behavior>
    - RED: text `/start` returns 200 JSON with friendly help text per D-13.
    - RED: text with no valid URL returns 200 JSON with friendly help text per D-03/D-08.
    - RED: text containing exactly one valid URL calls `submitJobUrl(url)` and maps new-job result to 202 JSON per D-01/D-02/D-08.
    - RED: duplicate result from `submitJobUrl` returns the existing job/status fields and does not spawn because helper controls duplicate behavior per D-04.
    - RED: text containing zero or multiple URLs does not create a job; multiple URLs receive help instructing the user to send one job posting URL.
  </behavior>
  <action>Parse only `update.message.text`; ignore non-message update types with a 200 handled response. Implement URL extraction with `URL` validation. Accept exactly one URL. For expected bad/non-job user messages return 200 so Telegram will not retry. For accepted new job submissions return 202. For duplicates, preserve the helper result body/status behavior. Do not store `message.chat.id` from incoming updates per D-14.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/routes/telegram.test.ts</automated>
  </verify>
  <done>Webhook supports `/start`, friendly help, one-URL submission, duplicate reuse, and no chat ID auto-storage.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: mount Telegram route outside API-key middleware</name>
  <files>api/src/index.ts, api/src/app.test.ts</files>
  <behavior>
    - RED: `POST /telegram/webhook` with valid Telegram secret but no `X-API-Key` reaches the Telegram route rather than returning API-key 401.
    - RED: `/jobs/*` and `/profile/*` still return 401 without `X-API-Key`.
  </behavior>
  <action>Import Telegram route in `api/src/index.ts` and mount `app.route('/telegram', telegram)` after global logger and before/after existing routes without adding API-key middleware for `/telegram/*`. Keep existing `/profile/*` and `/jobs/*` middleware unchanged. Add app-level regression coverage.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/app.test.ts src/routes/telegram.test.ts</automated>
  </verify>
  <done>`/telegram/webhook` is reachable with only Telegram secret auth; existing API-key gates remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram→/telegram/webhook | Public unauthenticated internet request until secret header is verified. |
| Webhook body→job submission | Untrusted text becomes a pipeline URL only after validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | S | api/src/routes/telegram.ts | mitigate | Require exact `X-Telegram-Bot-Api-Secret-Token` match against `TELEGRAM_WEBHOOK_SECRET`. |
| T-04-03-02 | T | api/src/routes/telegram.ts | mitigate | Validate extracted URL with `new URL()` and require exactly one URL before submission. |
| T-04-03-03 | D | api/src/routes/telegram.ts | mitigate | Duplicate URL submissions reuse shared duplicate behavior and do not spawn another pipeline. |
| T-04-03-04 | I | api/src/routes/telegram.ts | mitigate | 503/401 responses do not reveal configured secret or bot token. |
| T-04-03-05 | E | api/src/index.ts | mitigate | Do not add API-key bypass to `/jobs/*` or `/profile/*`; only `/telegram/*` uses Telegram-specific auth. |
</threat_model>

<verification>
Run `cd api &amp;&amp; npm test -- src/routes/telegram.test.ts src/app.test.ts` and `cd api &amp;&amp; npm run build`.
</verification>

<success_criteria>
`POST /telegram/webhook` satisfies D-01 through D-08, D-13, D-14, TELEGRAM-01, TELEGRAM-02, and TELEGRAM-05 without changing existing route authentication.
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-bot-integration/04-03-SUMMARY.md`
</output>
