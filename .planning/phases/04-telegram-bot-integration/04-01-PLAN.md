---
phase: 04-telegram-bot-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/job-submission.ts
  - api/src/job-submission.test.ts
  - api/src/routes/jobs.ts
  - api/src/routes/jobs.test.ts
autonomous: true
requirements: [TELEGRAM-01]
must_haves:
  truths:
    - "Existing POST /jobs behavior still accepts a URL, creates a job, spawns the pipeline, and returns 202 with status_url."
    - "Existing duplicate URL behavior still returns the existing job/status without spawning another pipeline process."
    - "Telegram webhook implementation has a reusable job-submission helper to call instead of duplicating POST /jobs logic."
  artifacts:
    - path: "api/src/job-submission.ts"
      provides: "Shared submitJobUrl helper for API and Telegram job intake"
      exports: ["submitJobUrl", "statusUrl"]
    - path: "api/src/job-submission.test.ts"
      provides: "TDD coverage for new shared job submission behavior"
    - path: "api/src/routes/jobs.ts"
      provides: "POST /jobs delegates to shared helper"
    - path: "api/src/routes/jobs.test.ts"
      provides: "Regression coverage for unchanged POST /jobs API behavior"
  key_links:
    - from: "api/src/routes/jobs.ts"
      to: "api/src/job-submission.ts"
      via: "submitJobUrl import and call"
      pattern: "submitJobUrl"
    - from: "api/src/job-submission.ts"
      to: "run_agents.py"
      via: "spawn('uv', ['run', 'python', 'run_agents.py', url, '--job-id', jobId])"
      pattern: 'run_agents\.py'
---

<objective>
Extract the existing job URL submission path into a reusable helper without changing public `/jobs` behavior.

Purpose: Telegram webhook intake must submit URLs through the same job creation, duplicate handling, and subprocess spawn behavior as `POST /jobs` per D-01 and D-04.
Output: Shared TypeScript helper plus unchanged `/jobs` route regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-telegram-bot-integration/04-CONTEXT.md
@.planning/phases/04-telegram-bot-integration/04-RESEARCH.md
@api/src/routes/jobs.ts
@api/src/routes/jobs.test.ts
@api/src/errors.ts

<interfaces>
From `api/src/routes/jobs.ts`:
```typescript
function statusUrl(jobId: string) {
  return `/jobs/${jobId}/status`
}

// Current POST /jobs behavior to preserve:
// - parse JSON body { url }
// - validate non-empty valid URL
// - SELECT job_id,status FROM jobs WHERE job_url = $1
// - duplicate returns 409 with { error, job_id, status, status_url }
// - INSERT INTO jobs (job_url, status) VALUES ($1, 'new') RETURNING job_id
// - spawn('uv', ['run', 'python', 'run_agents.py', url, '--job-id', job_id], detached/unref)
// - return { job_id, status: 'new', status_url } with HTTP 202
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: extract shared job submission helper</name>
  <files>api/src/job-submission.ts, api/src/job-submission.test.ts</files>
  <behavior>
    - RED: `submitJobUrl("https://example.com/job")` checks duplicates, inserts a new `jobs` row, spawns `uv run python run_agents.py <url> --job-id <id>`, unrefs the child, and returns `{ statusCode: 202, body: { job_id, status: "new", status_url } }`.
    - RED: duplicate URL returns `{ statusCode: 409, body: { error, job_id, status, status_url } }` and does not spawn.
    - RED: invalid URL returns `{ statusCode: 400, errorCode: "bad_request", title: "url must be a valid URL." }` without querying or spawning.
  </behavior>
  <action>Create `api/src/job-submission.ts` exporting `statusUrl(jobId: string)` and `submitJobUrl(url: string)`. Move the existing POST /jobs duplicate lookup, insert, spawn, unref, and child error handler into this helper exactly; preserve parameterized SQL and the `PROJECT_ROOT` resolution. Represent route errors as a discriminated result object so callers can map to `httpError` without throwing for expected bad input. Do not add dependencies. This implements D-01/D-04 foundation.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/job-submission.test.ts</automated>
  </verify>
  <done>Shared helper covers new, duplicate, invalid URL, and spawn-error DB write behavior with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: delegate POST /jobs to shared helper</name>
  <files>api/src/routes/jobs.ts, api/src/routes/jobs.test.ts</files>
  <behavior>
    - RED: existing route tests for create, duplicate, and invalid URL still assert the same response status/body.
    - RED: `POST /jobs` uses `httpError` for helper bad-request results and `c.json(result.body, result.statusCode)` for success/duplicate results.
  </behavior>
  <action>Refactor `api/src/routes/jobs.ts` so `jobs.post('/')` only parses JSON/body shape, calls `submitJobUrl(body.url)`, and maps the helper result to the same existing HTTP responses. Remove route-local `spawn`, `resolve`, `PROJECT_ROOT`, and `statusUrl` logic after the helper owns it. Keep all non-POST job routes untouched.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/routes/jobs.test.ts src/job-submission.test.ts</automated>
  </verify>
  <done>`POST /jobs` responses and side effects are unchanged, and no duplicate job-submission implementation remains in `routes/jobs.ts`.</done>
</task>

<task type="auto">
  <name>task 3: build-check shared helper refactor</name>
  <files>api/src/job-submission.ts, api/src/routes/jobs.ts</files>
  <action>Run the TypeScript build and fix any strict type issues introduced by the helper result union. Keep the helper's public contract explicit with exported TypeScript interfaces/types so downstream Telegram route code can consume it without inference scavenging.</action>
  <verify>
    <automated>cd api &amp;&amp; npm run build</automated>
  </verify>
  <done>Build passes and `submitJobUrl`/`statusUrl` are exported with stable types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client→API route | Untrusted URL input crosses into job creation. |
| API→Postgres | Route/helper writes job state. |
| API→OS subprocess | Helper starts the Python pipeline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | T | api/src/job-submission.ts | mitigate | Validate URL with `new URL()` before DB/spawn. |
| T-04-01-02 | I | api/src/job-submission.ts | mitigate | Never include subprocess stderr or environment secrets in HTTP responses. |
| T-04-01-03 | D | api/src/job-submission.ts | mitigate | Duplicate URL lookup prevents duplicate pipeline subprocesses for same URL. |
| T-04-01-04 | T | api/src/job-submission.ts | mitigate | Use parameterized `$1` SQL for URL/job_id values only. |
</threat_model>

<verification>
Run `cd api &amp;&amp; npm test -- src/routes/jobs.test.ts src/job-submission.test.ts` and `cd api &amp;&amp; npm run build`.
</verification>

<success_criteria>
The shared helper exists, `POST /jobs` delegates to it, all existing job route behavior remains identical, and Telegram plans can import `submitJobUrl` for D-01/D-04.
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-bot-integration/04-01-SUMMARY.md`
</output>
