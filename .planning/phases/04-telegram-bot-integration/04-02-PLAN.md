---
phase: 04-telegram-bot-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/src/telegram.ts
  - api/src/telegram.test.ts
autonomous: true
requirements: [TELEGRAM-03, TELEGRAM-05]
user_setup:
  - service: telegram
    why: "Outbound pipeline result notifications via Telegram Bot API"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather token for the Rendure bot"
must_haves:
  truths:
    - "Approved-job notification text includes status, QA score, role/company when available, and Markdown/PDF API paths."
    - "Low-match notification text includes status, QA score, and top high-severity QA gaps when available."
    - "Error notification text is safe and omits raw stack traces/internal exception details."
    - "Missing TELEGRAM_BOT_TOKEN disables outbound send behavior without crashing."
  artifacts:
    - path: "api/src/telegram.ts"
      provides: "Telegram message formatting, MarkdownV2 escaping, configuration, and sendMessage client"
      exports: ["formatTelegramTerminalMessage", "sendTelegramMessage", "isTelegramBotConfigured"]
    - path: "api/src/telegram.test.ts"
      provides: "TDD coverage for formatting, escaping, config, and fetch behavior"
  key_links:
    - from: "api/src/telegram.ts"
      to: "Telegram Bot API"
      via: "fetch https://api.telegram.org/bot<TOKEN>/sendMessage"
      pattern: 'api\.telegram\.org/bot'
    - from: "api/src/telegram.ts"
      to: "Phase 3 resume routes"
      via: "message paths /jobs/:id/resume/:version_id and /pdf"
      pattern: "/jobs/.*/resume/.*/pdf"
---

<objective>
Create the reusable Telegram outbound message and Bot API client layer.

Purpose: Terminal notifications need deterministic, safe message text and a small dependency-free Bot API sender before listener wiring is added.
Output: `api/src/telegram.ts` with formatting, escaping, config detection, and sendMessage behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-telegram-bot-integration/04-CONTEXT.md
@.planning/phases/04-telegram-bot-integration/04-RESEARCH.md
@api/src/sse.ts
@api/src/errors.ts

<interfaces>
Create this exported contract for downstream plan 04:
```typescript
export interface TelegramTerminalJob {
  job_id: string
  status: 'approved' | 'low_match' | 'error'
  qa_score: string | number | null
  company_name: string | null
  role_title: string | null
  active_resume_id: string | null
  gaps?: Array<{ category?: string; detail?: string; severity?: string }> | null
}

export function isTelegramBotConfigured(): boolean
export function formatTelegramTerminalMessage(job: TelegramTerminalJob): string
export async function sendTelegramMessage(chatId: string, text: string): Promise<{ ok: true } | { ok: false; error: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement terminal message formatting</name>
  <files>api/src/telegram.ts, api/src/telegram.test.ts</files>
  <behavior>
    - RED: approved job with `active_resume_id` formats status, QA score, role/company, `/jobs/:id/resume/:version_id`, and `/jobs/:id/resume/:version_id/pdf` per D-09/D-12.
    - RED: low_match formats status, QA score, and at most the top high-severity gaps sorted from supplied gap order per D-10.
    - RED: error formats safe failure text with job ID/status path and does not include metadata.detail/stack-like strings per D-11.
    - RED: dynamic values containing MarkdownV2 punctuation are escaped before inclusion per D-15.
  </behavior>
  <action>Create `formatTelegramTerminalMessage` and MarkdownV2 escaping helpers in `api/src/telegram.ts`. Use API paths only, not absolute URLs, per D-12. Include Markdown/PDF resume paths only when `active_resume_id` exists. Do not introduce inline keyboards, `/status`, chat ID capture, or absolute public URL config because those are deferred. Keep wording concise and deterministic.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/telegram.test.ts</automated>
  </verify>
  <done>Approved, low_match, and error formatters produce escaped, deterministic text satisfying D-09 through D-12 and D-15.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement Telegram sendMessage client</name>
  <files>api/src/telegram.ts, api/src/telegram.test.ts</files>
  <behavior>
    - RED: missing `TELEGRAM_BOT_TOKEN` makes `isTelegramBotConfigured()` false and `sendTelegramMessage` returns `{ ok: false, error: "telegram_not_configured" }` without fetch per TELEGRAM-05.
    - RED: configured token sends POST JSON to `https://api.telegram.org/bot<TOKEN>/sendMessage` with `chat_id`, `text`, and `parse_mode: "MarkdownV2"`.
    - RED: non-ok Telegram response returns `{ ok: false, error: "telegram_send_failed" }` without exposing the bot token.
  </behavior>
  <action>Implement `sendTelegramMessage` using built-in `fetch`; do not add a package. Read `process.env.TELEGRAM_BOT_TOKEN` at call time so tests can mutate env safely. Return typed results instead of throwing for expected configuration/API failures. Never include the token in returned errors or logs. This implements TELEGRAM-05's non-crashing configuration behavior for outbound notifications.</action>
  <verify>
    <automated>cd api &amp;&amp; npm test -- src/telegram.test.ts</automated>
  </verify>
  <done>Outbound Telegram client is dependency-free, tested for configured/unconfigured/failure paths, and does not leak secrets.</done>
</task>

<task type="auto">
  <name>task 3: build-check Telegram helper exports</name>
  <files>api/src/telegram.ts</files>
  <action>Run the build and ensure all exported types/functions are explicit and usable by the later notification listener without importing test-only helpers.</action>
  <verify>
    <automated>cd api &amp;&amp; npm run build</automated>
  </verify>
  <done>Build passes and `api/src/telegram.ts` exposes stable formatting/send contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database→Telegram text | Stored job/QA data crosses into user-visible Markdown. |
| API server→Telegram Bot API | Server sends HTTPS request with bot token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | I | api/src/telegram.ts | mitigate | Never include `TELEGRAM_BOT_TOKEN` in returned errors/log strings. |
| T-04-02-02 | T | api/src/telegram.ts | mitigate | Escape all dynamic MarkdownV2 content before sending. |
| T-04-02-03 | I | api/src/telegram.ts | mitigate | Error messages omit stack traces/internal exception detail per D-11. |
| T-04-02-04 | D | api/src/telegram.ts | accept | Telegram API outage causes missed notification attempt; no retry queue in Phase 4 scope. Return typed failure for listener logging. |
</threat_model>

<verification>
Run `cd api &amp;&amp; npm test -- src/telegram.test.ts` and `cd api &amp;&amp; npm run build`.
</verification>

<success_criteria>
Telegram message text and send client are implemented, tested, safe, and ready for terminal listener integration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-bot-integration/04-02-SUMMARY.md`
</output>
